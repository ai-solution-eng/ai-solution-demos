apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "gradio-vlm-chart.name" . }}-shared-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gradio-vlm-chart.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: {{ .Values.storage.shared.capacity }}
  storageClassName: {{ .Values.storage.shared.storageClassName }}
  volumeName: {{ include "gradio-vlm-chart.name" . }}-shared-pv
  volumeMode: Filesystem
